name: atvd_crunchyroll
description: Projeto Crunchyroll Flutter
publish_to: "none"
version: 1.0.0+1
environment:
  sdk: ">=3.5.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  supabase_flutter: ^2.9.1
  cached_network_image: ^3.4.1
  google_fonts: ^6.3.0
  image_picker: ^1.1.2
  file_picker: ^10.3.2

  flutter_staggered_grid_view: ^0.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/